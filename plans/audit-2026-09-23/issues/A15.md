# [P1][A15] 진행 중 재조정에 들어온 변경을 유실 없이 반영하고 데이터 교체와 알림 작업을 직렬화

## 문제와 사용자 영향

알림 재조정이 일정 목록을 읽은 뒤 새 일정이 저장되면, 새 호출은 진행 중 Future에 합류하고 새 목록을 읽지 않는다. 첫 실행이 오래된 목록으로 OS 예약과 registry를 저장하면 새 일정의 알림이 빠지거나 수정 전 시각·내용이 남을 수 있다. 삭제·완료의 표적 취소 역시 재조정과 별도로 실행되어 오래된 재조정이 삭제된 예약을 다시 쓰거나 취소 증거를 덮을 수 있다. 상세 표시 OFF도 같은 경합에서 최신 선택이 적용되지 않을 수 있다.

Backup Restore/Local Data Reset과 겹치면 영향이 더 크다. registry 저장 직전에 세대만 확인하는 것으로는 이미 OS에 보낸 구세대 예약이 데이터 교체 이후 늦게 성공하는 것을 막지 못한다. 새 데이터에 같은 일정 ID가 있으면 구세대 정리가 신세대 예약을 취소하는 역방향 경합도 생긴다.

감사 판정은 **P1 · 경쟁 조건 확인, 결정적 재현 필요**다. 아래 초기 명세는 읽기 전용 조사와 실제 grill 문답으로 확정했다. 후속 구현과 최종 로컬795 tests/analyze 검증은 하단 실행 기록에 추가했다. 원격 CI와 실제 Android/iOS 검증은 별도 확인하며 실기기 완료를 주장하지 않는다.

## 조사 기준과 코드 근거

감사 기준은 `44067d7ab26b6290c16cdb13b99f57387dec08f6`이며, 아래 링크와 동작 조사는 반복 일정 upstream merge `2b02fb77`이 통합된 **`712b37081f95ea30aa6d1c61072d167ddaec78de`**에 고정했다. 조사 중 A12가 같은 working tree에서 gate의 ChangeNotifier/isAvailable 및 알림 탭 경로를 변경하고 있으므로 이를 이미 커밋된 A15 선행 구현으로 간주하지 않았다. 구현 전에 A12→A13→A14의 최종 코드와 인터페이스를 다시 읽어 통합한다.

| 근거 | 확인 내용 |
|---|---|
| [ReconcileAlarmsUseCase:56](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/domain/use-cases/reconcile_alarms_use_case.dart#L56) | `_inFlight`가 있으면 그대로 반환한다. 요청 순번·dirty·후속 실행이 없다. |
| [일정 snapshot:142](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/domain/use-cases/reconcile_alarms_use_case.dart#L142) / [OS 예약:256](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/domain/use-cases/reconcile_alarms_use_case.dart#L256) / [registry 교체:292](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/domain/use-cases/reconcile_alarms_use_case.dart#L292) | snapshot 이후 여러 await와 side effect가 이어져 새 mutation이 끼어들 수 있다. 마지막 replaceAll은 그 실행에서 모은 record를 쓴다. |
| [ScheduleMutationAlarmEffectsCoordinator:21](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/domain/use-cases/schedule_mutation_alarm_effects_coordinator.dart#L21) | 삭제·완료의 표적 취소를 먼저 await한 뒤 reconcile을 fire-and-forget으로 요청한다. 자체 reconcile 오류는 관찰하지만 전체 writer를 직렬화하지 않는다. |
| [CancelScheduleAlarmUseCase:19](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/domain/use-cases/cancel_schedule_alarm_use_case.dart#L19) / [CancelAllAlarmsUseCase](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/domain/use-cases/cancel_all_alarms_use_case.dart) | 별도 registry 조회·OS 취소·registry 삭제를 실행한다. 취소 오류를 무시하는 D03 결함도 남아 있어 공유 경계에 옮긴다는 이유로 실패 증거를 지우면 안 된다. |
| [AlarmRegistryRepositoryImpl:18](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/data/repositories/alarm_registry_repository_impl.dart#L18) | upsert/deleteByScheduleId는 loadAll→replaceAll이다. 서로 다른 writer가 겹치면 다른 작업의 최소 소유권 record도 덮을 수 있다. |
| [LocalDataOperationGate:12](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/core/database/local_data_operation_gate.dart#L12) | 기존 generation은 replacesData 작업 시작 및 invalidate에서 증가한다. reconcile은 이 경계에 연결되지 않았다. |
| [BackupService](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/core/backup/backup_service.dart) / [LocalDataResetService](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/core/database/local_data_reset_service.dart) / [LocalDataLifecycle](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/core/database/local_data_lifecycle.dart) | restore/reset은 gate를 사용한다. reset은 DB를 닫고 invalidate한다. 완전한 취소 실패/재시작 복구 프로토콜은 D03 의존이다. |
| [실제 준비 완료:168](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/domain/use-cases/schedule_preparation_session_use_case.dart#L168) | DB 완료→별도 표적 취소→runtime 정리→catch 없는 unawaited reconcile 경로다. coordinator만 고치면 이 경로가 빠진다. |
| [AuthBloc:79](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/presentation/app/bloc/auth/auth_bloc.dart#L79) / [App](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/presentation/app/screens/app.dart) | startup/resume에 fire-and-forget 호출이 있다. 세대 무효화와 예외를 미처리 Future로 남기지 않아야 한다. |
| [RecurringSchedulesUseCase](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/domain/use-cases/recurring_schedules_use_case.dart) / [AlarmRepositoryImpl:55](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/lib/data/repositories/alarm_repository_impl.dart#L55) | 반복 생성·변경·삭제는 mutation coordinator를 거친다. getAlarmWindow 자체가 회차 materialize를 호출하므로 전역 DB watcher는 자기 재실행을 만들 수 있다. |
| [기존 coalescing 테스트:801](https://github.com/DevKor-github/OnTime-front/blob/712b37081f95ea30aa6d1c61072d167ddaec78de/test/domain/use-cases/reconcile_alarms_use_case_test.dart#L801) | 동시 두 call의 결과 동일 및 window 조회 1회만 확인한다. snapshot 뒤 변경·각 호출의 완료 의미를 검사하지 않아 결함을 정상으로 고정할 수 있다. |

## 도메인과 기존 결정

- Local-only OnTime은 Android/iOS 제품이다. 서버·로그인·원격 push·분석 수집을 추가하지 않는다(ADR 0011, 0016).
- Schedule은 원본 로컬 일정이고 Schedule Notification/Alarm은 재구성 가능한 전달 상태다. DB 저장 성공과 OS 예약 성공은 같은 사건이 아니다.
- ADR 0019에 따라 현재 유효한 미래 Schedule/반복 Schedule Occurrence 중 가장 가까운 대상부터 플랫폼 용량 60개를 적용한다. 이미 지난·완료·삭제 대상을 재생하지 않는다.
- ADR 0013은 restore의 원자적 데이터 교체 후 미래 알림 재구성을, ADR 0020은 reset의 구세대 상태 제거를 요구한다.
- ADR 0030과 A05/A11의 private 기본값, 원문 최소화, 취소 ownership/tombstone을 유지한다. 재조정 경합을 해결하면서 상세 OFF 선택을 되돌리거나 원문을 새 큐에 저장하지 않는다.
- A13의 actual exact/inexact 결과와 A14의 present/absent/unknown·provider 교체·부분 실패 계약을 유지한다. 최종 registry 존재를 OS 도착 보장으로 해석하지 않는다.
- request revision, dirty, drain, generation은 구현 조정 개념이다. 새로운 제품 용어나 ADR는 필요하지 않으며 CONTEXT를 구현 명세 저장소로 사용하지 않는다.

## 확정한 처리 계약

### 요청 접수와 각 Future의 의미

1. 한 알림 조정 소유자가 side effect를 실행한다. 요청 접수는 첫 await 이전에 동기적으로 메모리 순번을 증가시키고 해당 호출의 대기자를 등록한다.
2. pass 시작 때 그 pass가 책임지는 revision cutoff를 캡처한다. 같은 세대에서 cutoff 이후 새 요청이 접수되면 다음 pass가 현재 DB·설정·권한·언어·시간대를 새로 읽는다.
3. burst는 하나의 후속 pass로 합쳐도 된다. 후속 pass 도중 다시 변경이 들어오면 또 다음 pass가 필요하다. 단순 `_inFlight` Future 반환으로 새 요청을 소비하지 않는다.
4. 각 call은 최소 자기 요청까지 포함한 pass의 결과 또는 실패로 정확히 한 번 완료한다. 첫 pass의 오래된 결과로 다음 revision의 호출을 완료하지 않는다.
5. 모든 대기자를 전역 quiet 시점까지 붙잡지 않는다. 계속 편집하더라도 이미 처리한 cutoff의 대기자는 해당 결과로 끝나고 새 요청은 다음 pass가 맡는다.
6. 마지막 요청 검사·idle 전환·새 drain 시작 사이의 경합도 유실 없이 처리한다. 구현의 동기 상태 전이와 finally 정리에서 이를 보장한다.
7. 일반 저장 UI는 OS 예약 완료를 기다리도록 확대하지 않는다. commit 이후 요청 접수는 반드시 이뤄지고 비동기 오류는 관찰한다. 이미 성공한 DB 저장을 알림 실패 때문에 롤백하지 않는다.
8. request revision은 프로세스 내 접수 순번이다. 백업 freshness용 durable dataRevision, DB schema version, lifecycle generation과 혼용하지 않는다.

### 실패와 재시도

| 상황 | 계약 |
|---|---|
| pass 성공 | 포함한 cutoff까지만 최신 해당 결과로 완료한다. |
| pass 부분 실패/조회·OS·registry 예외 | 해당 cutoff 대기자를 실패/부분 결과로 완료한다. 무기한 성공 대기로 붙잡지 않는다. |
| 실패 도중 실제 새 요청 접수 | 새 요청을 유실하지 않고 최신 상태로 다음 pass를 수행한다. |
| 새 요청 없이 실패만 반복 | 오류만으로 dirty를 재설정하지 않는다. 다음 시작/resume/명시적 retry/실제 mutation까지 멈춘다. |
| provider 성공 후 registry 저장 실패 | armed 성공으로 위조하지 않는다. 가능한 최소 ownership/tombstone을 보존한다. |
| 영속 저장소 자체 실패 | 메모리의 최소 ownership과 실패를 보존하고 A14 후속 관측으로 복구한다. 프로세스 사망까지 보장한다고 쓰지 않는다. |
| generation 변경 | 일반 OS 실패와 구별되는 typed invalidation 결과/예외로 구세대 대기자를 완료한다. |
| provider 무응답/timeout | 성공·취소 완료로 바꾸지 않는다. timeout이 native 호출을 실제 취소한다는 가정을 금지한다. 늦은 반환도 ownership/정리 경계가 회수해야 한다. |

startup/resume, 실제 준비 완료, mutation coordinator 등 fire-and-forget 호출부는 failure/invalidation을 관찰한다. raw 일정·준비 내용이나 내부 예외를 사용자 문구/로그에 추가하지 않는다. UI의 기존 결과 모델에는 실제 예약 실패/확인 필요를 연결하되 일반 설정 UI 전체 개편은 U03과 구분한다.

### 취소·restore/reset의 직렬 경계

1. reconcile의 OS 작업 및 registry 저장, 표적 취소, 전체 취소, restore/reset의 알림 정리는 작은 명시적 coordinator/port를 통한 하나의 직렬 소유권을 사용한다. 새 전역 작업 프레임워크나 중첩 lock을 도입하지 않는다.
2. 기존 `LocalDataOperationGate.generation`을 재사용한다. A12의 최종 readiness/ChangeNotifier 인터페이스와 통합하고 별도의 데이터 세대 체계를 만들지 않는다.
3. replacement 시작 즉시 구세대 신규 예약 작업의 접수를 막고 대기자를 invalidated로 완료한다. 아직 provider에 보내지 않은 오래된 예약은 실행하지 않는다.
4. 이미 provider에 보낸 호출은 반환 결과를 회수하고 최소 ownership/취소 증거를 보존한다. 구세대 결과를 새 registry의 정상 예약으로 쓰지 않는다.
5. lifecycle은 구세대 in-flight side effect의 반환·정리 경계를 거친 뒤 DB 교체/완료를 허용한다. 데이터 교체 완료를 먼저 표시하고 늦은 구세대 알림을 방치하지 않는다.
6. 같은 stable ID의 신세대 예약은 구세대 호출·정리가 정리된 이후에만 수행한다. 구세대 cleanup이 새 등록을 취소할 수 있는 순서를 허용하지 않는다.
7. restore 후 재등록은 gate가 해제된 새 generation에서 현재 복원 DB를 읽는다. reset은 기존 재시작 정책에 따라 신규 예약을 막는다.
8. gate를 소유한 restore가 gate 해제를 기다리는 drain을 다시 await하는 교착을 만들지 않는다. lifecycle quiesce/cleanup과 일반 재조정 진입을 분리해 테스트한다.
9. 취소 오류를 이미 없음으로 처리하거나 A05/A11 tombstone을 deleteAll/replaceAll로 지우지 않는다. 실패 시 성공하지 않은 정리를 완료로 표시하지 않는다. A14가 정한 플랫폼별 취소 증거 한계를 지킨다.
10. 전체 재시작 후 reset marker·프로세스 사망·디스크 오류의 crash consistency는 D03 의존이다. A15가 실행 중 경합을 막았다고 완전한 reset 복구까지 완료한 것으로 보고하지 않는다.

### 실제 요청 경로와 반복 일정

구현에서 현재 제품 경로를 다시 추적한다. 일반 일정 생성·수정·삭제, 실제 준비 완료, 반복 생성·회차/이후 수정·삭제, 알림 ON/OFF·상세 표시 변경, permission/locale/timezone 재조회, 시작/resume, restore 완료가 commit/refresh 이후 적절한 요청을 접수하는지 확인한다.

Default/Custom Preparation 저장은 현재 미래 일정의 준비 시작 시각·내용에 실제 영향을 주는 경로에 한해 같은 진입점을 연결한다. 무관한 템플릿 라이브러리 변경까지 전역 감시하지 않는다. 반복 회차 materialize나 registry 갱신 자체를 새 요청으로 돌리는 DB watcher, 주기 polling, 임의 TTL, 새 background scheduler를 만들지 않는다.

upstream recurrence 규칙·DB schema2·backup v2를 유지한다. nearest eligible 60개가 바뀌면 최종 집합이 최신 후보를 반영해야 한다. 한 pass 내 여러 DB 읽기의 transaction snapshot 일관성이 별도 결함이면 A08/C01 경계를 명시하되, committed mutation 후 실제 새 요청이 접수됐을 때 다음 pass가 최신 상태를 읽는 A15 계약은 반드시 보장한다.

## 수용 기준과 결정적 회귀

테스트는 sleep에 기대지 않고 Completer/barrier를 가진 blocking fake로 실제 snapshot/OS 호출/registry 저장 경계에 mutation을 끼운다. 요청 횟수만 검사하지 말고 provider의 최종 owned 예약 집합·내용·시각·registry·각 호출 Future 결과를 함께 확인한다.

| 시나리오 | 기대 결과 |
|---|---|
| 최초 snapshot 읽기 후 일정 생성 | 첫 snapshot에 없던 일정이 후속 pass에 예약되고 새 호출은 첫 결과로 끝나지 않는다. |
| snapshot 뒤 시각/제목/준비 변경 | 최신 시각·허용 content로 수렴한다. 오래된 결과가 마지막 상태로 남지 않는다. |
| snapshot 뒤 삭제/완료 | 표적 취소와 오래된 재등록이 교차하지 않고 최종 예약에서 제외된다. |
| snapshot 뒤 상세 ON→OFF 또는 알림 OFF | 최신 선택 유지, 취소 증거 보존, 상세 과거 등록을 private/disabled 성공으로 오판하지 않는다. |
| 첫 pass 중 burst, 두 번째 pass 중 또 변경 | 필요한 trailing pass를 수행하고 각 cutoff 대기자를 정확히 한 번 완료한다. |
| 지속적으로 새 요청 접수 | 이미 처리된 revision의 대기자를 quiet까지 무한 대기시키지 않는다. |
| pass 종료/finally 경계에서 새 요청 | 신규 drain 또는 후속 pass가 담당하며 유실·중복 완료가 없다. |
| snapshot/OS/registry 실패 중 새 요청 | 실패는 해당 cutoff로 반환하고 새 요청은 최신 pass에서 처리한다. |
| 실패 단독, 새 요청 없음 | 즉시 무한 반복 없이 종료한다. 이후 명시적 요청은 다시 실행된다. |
| provider 성공 후 registry 저장 실패 | 성공으로 보고하지 않고 최소 ownership 및 복구 필요 상태를 유지한다. |
| 표적 취소와 replaceAll 경합 | unrelated 예약과 cancellationPending을 덮거나 부활시키지 않는다. |
| reset/restore가 snapshot 또는 OS await 중 시작 | 구세대 Future invalidation, 새 구세대 작업 차단, 이미 전송한 호출의 늦은 결과 회수·정리를 검증한다. |
| 구세대·신세대 동일 stable ID | 구세대 cleanup 종료 뒤 신세대 등록, 새 예약을 늦은 취소가 지우지 않는다. |
| restore gate 소유 중 조정 정리 | gate 해제 대기와 상호 await 교착 없이 정리 후 새 세대 재조회한다. |
| reset invalidate 후 요청 | 닫힌 DB를 읽거나 새 알림을 예약하지 않고 명시적인 무효화/사용 불가로 끝난다. |
| 취소 실패/관측 unknown/late provider 반환 | 정상 완료 위조 없음, 최소 증거 유지, A14 기준에 맞게 후속 복구한다. |
| 반복 회차 수정·이후 삭제·nearest 60 교체 | 최신 eligible 집합 반영, 과거/완료 회차 재생 없음, materialize self-dirty loop 없음. |
| actual timing/provider 변경이 실행 중 발생 | A13/A14의 새 actual receipt·관측 결과를 보존하고 이전 pass 결과로 덮지 않는다. |
| startup/resume/실제 완료 fire-and-forget 실패 | unhandled Future 없음, DB 저장 성공과 OS 실패를 구분한다. |
| 개인정보·관련 없는 알림 | 최소 payload/tombstone 유지, raw fingerprint 재도입 및 무차별 cancelAll 없음. |

- [ ] 기존 `coalesces overlapping reconciliation requests`의 무조건 조회 1회 기대를 실제 의도 기반 테스트로 바꿨다. 요청이 어느 pass cutoff에 포함되는지에 따라 합쳐질 수 있으나 유실은 허용하지 않는다.
- [ ] call Future의 result/error/invalidation 및 completer 정확히 한 번 완료를 직접 검증했다.
- [ ] 가장 중요한 snapshot 후 mutation 회귀가 수정 전 실패하고 수정 후 통과함을 확인했다.
- [ ] 실제 mutation use case와 coordinator, 취소 use case, lifecycle gate를 통합한 회귀를 실행했다. fake callCount만으로 실제 경계 통합을 통과 처리하지 않았다.
- [ ] A05 privacy cancellation, A11 최소 원문 경계, A12 세대 무효화, A13 actual timing, A14 provider/관측 계약의 관련 회귀를 실행했다.
- [ ] 필요한 generator를 로컬에서 재생성하고 관련 Flutter unit/widget tests, `flutter analyze`, full `flutter test` 결과를 실행 SHA에 연결했다. generated Dart는 커밋하지 않는다.
- [ ] Android/iOS에서 여러 빠른 저장·OFF 변경·restore/reset 겹침 후 실제 pending/provider 및 잠금화면 전달을 확인했다. fake OS 상태, 플랫폼 요청 성공, pending 관측, 실제 도착을 구분해 기록했다.
- [ ] 실기기 미실행·provider 무응답·플랫폼 관측 한계는 그대로 남겼다. Android plugin cache를 AlarmManager 생존 증거로 부르지 않는다.

## 의존성과 완료 경계

| 관련 항목 | A15의 책임과 경계 |
|---|---|
| A05 | 실행 중 들어온 private/OFF 선택을 후속 pass에 반영한다. 취소 실패 tombstone/실패 상태를 보존한다. |
| A11 | 큐·실패·registry에 raw title/준비 fingerprint를 재저장하지 않는다. privacy migration과 writer 경합을 직렬 소유권에 연결한다. |
| A12 | 기존 generation/readiness 최종 인터페이스를 재사용한다. 알림 탭 처리 자체의 정책은 변경하지 않는다. |
| A13 | actual exact/inexact 결과를 각 pass의 실제 결과로 보존한다. timing 권한 정책을 다시 설계하지 않는다. |
| A14 | provider 교체/관측/취소 확인과 같은 소유권 경계를 사용한다. unknown을 성공으로 바꾸거나 중복 provider 등록을 허용하지 않는다. |
| A09/D03 | restore runtime 정리와 reset 재시작 복구에 필요한 직렬 경계를 제공한다. 완전한 crash consistency를 A15 완료로 주장하지 않는다. |
| A08/C01 | 저장 transaction과 여러 읽기의 snapshot 일관성은 별도 점검 대상이다. 이미 commit된 변경의 재조정 요청 유실은 A15 책임이다. |
| U03 | 저장 성공/OS 적용 실패를 구별하는 결과를 제공한다. 일반 설정 UI의 전면 재설계는 제외한다. |
| upstream 반복 일정 | 회차 materialize·가장 가까운 60개·schema2·backup v2를 보존한다. 재조정 자체가 무한 재실행을 만들지 않게 검증한다. |

초기 grill 단계에서는 읽기 전용 조사와 이 문서만 작성했다. 후속 구현 및 검증 결과는 하단 실행 기록과 validation receipt에 구분해 추가했다.

## grill-with-docs 실제 전체 문답

사용자가 지정한 방식에 따라 A15 전담 에이전트가 `/Users/ejunpark/.agents/skills/grill-with-docs/SKILL.md`를 읽고, root에게 한 번에 하나씩 질문했으며 root가 사용자 대신 답했다. 아래 질문·답변은 요약하지 않고 실제 전달 문구를 보존했다.

### Q1 — 후속 요청과 Future 완료 계약

**A15 전담 질문**

> A15 전담 조사에서 grill-with-docs를 읽고 적용했습니다. 712b3708의 call은 진행 중 요청을 return running으로 끝내고, snapshot 뒤 registry replaceAll까지 추가 요청 표식이 없어 유실 가능합니다. A15 Q1 — 일정 snapshot 읽기 뒤 생성·수정·삭제/완료나 알림 OFF/내용 설정/권한 변경 요청이 들어오면, 추천은 같은 세대의 단일 drain에서 request revision/dirty를 기록하고 최신 상태로 다시 실행하며, 각 call의 Future는 자기 요청을 반영한 pass(또는 그보다 최신의 안정된 pass)가 끝난 뒤 최신 결과로 완료하는 것입니다. UI DB 저장은 OS 성공을 기다리지 않는 현재 best-effort를 유지하되 request 접수는 저장 후 반드시 이뤄져야 합니다. 매 요청별 OS 작업 큐 대신 burst를 합치면서 유실 없는 trailing rerun, 후속 pass 중 새 변경도 다시 반영하는 정책으로 확정할까요?

**Root 답변**

> Q1 답변: 추천한 단일 drain + request revision/dirty + trailing rerun 정책으로 확정합니다. 요청 접수 시 revision을 동기적으로 증가시켜 await 이전/종료 직전 경합도 놓치지 않게 하고, 진행 중 새 요청은 최신 상태를 다시 읽는 다음 pass로 반영합니다. 각 call의 Future는 최소 자기 요청을 포함한 pass가 끝난 뒤 완료해야 하며, 최초 pass의 오래된 결과로 후속 호출을 완료하면 안 됩니다. burst는 합쳐도 되지만 후속 pass 중 들어온 변경도 다시 실행해야 합니다. 모든 대기자를 무조건 전역 quiet 시점까지 붙잡아 지속 편집에 무한 대기시키는 설계는 피하고, 각 revision까지 반영한 결과/실패를 명확히 전달하세요. DB 저장 성공과 OS 예약 성공은 구분하며 일반 저장 UI는 기존 best-effort 경계를 유지합니다. 다만 commit 뒤 reconcile 요청 접수 자체가 유실되거나 Future 오류가 미처리되는 호출부는 실제 사용 경로에서 보완해야 합니다. generation 교체 및 실패 재실행의 무한 루프 방지는 다음 질문에서 따로 확정합시다. 기존 A05/A11 개인정보 취소와 A13/A14 결과/관측 계약을 보존하고, 매 요청별 OS 작업 큐나 새 background polling은 추가하지 않습니다.

### Q2 — 데이터 교체·취소와 늦은 OS 호출

**A15 전담 질문**

> A15 Q2 — generation만 완료 직전에 검사하면 이미 시작된 OS schedule이 reset/restore 정리 뒤 늦게 성공하여 구세대 알림이 부활할 수 있습니다. 현재 CancelSchedule/CancelAll은 reconcile과 별도 registry load→삭제 경로이며, gate는 replacement 시작에 generation을 올리지만 drain은 이를 모릅니다. 추천: 기존 LocalDataOperationGate.generation을 재사용하고 reconcile·표적/전체 취소·restore/reset의 알림 정리를 같은 단일 side-effect 직렬 경계에 연결합니다. replacement 시작 즉시 구세대 요청/대기자를 invalidated로 종료하고 신규 예약을 막되, 이미 platform에 보낸 호출의 반환과 최소 ownership/tombstone 기록·정리를 먼저 수습한 다음 lifecycle 취소/DB 교체 완료를 허용합니다(동일 안정 ID 재사용 시 구세대 cleanup이 신세대 예약을 지우지 않아야 함). restore 완료 후 새 generation으로 현재 DB만 재조회, reset은 재시작 전 신규 예약 금지. OS 실패 시 삭제 완료를 위조하지 않고 A05/A11 최소 취소 증거 유지, 중단 재개 reset 프로토콜 전체는 D03과 명시적 의존으로 둡니다. 이 경계로 확정할까요?

**A15 전담 추가 코드 근거**

> 추가 코드 근거: 기존 reconcile 테스트 :801은 두 동시 call의 결과 동일 + alarmWindowRequestCount==1만 강제해 dirty rerun 요구와 충돌하므로 의도 기반 회귀로 교체해야 합니다. actual finishSchedulePreparation :173-176은 DB 완료→별도 CancelSchedule→clear runtime→unawaited reconcile이고 startup AuthBloc/resume도 catch 없는 unawaited 경로가 있습니다. 문서에 이 경로까지 실패 관찰/요청 접수 범위로 명시하겠습니다. A12 WIP의 gate ChangeNotifier/isAvailable 변경은 읽었지만 baseline 712b3708의 단순 gate와 구분했습니다.

**Root 답변**

> Q2 답변: 그 경계로 확정합니다. 기존 gate generation을 재사용하고 알림 side effect의 직렬 소유권을 한곳에 모읍니다. 단순히 마지막 registry write만 generation 검사하는 수정은 불충분합니다. replacement 시작은 새 구세대 작업 접수를 막고 대기자를 명시적으로 invalidated 처리하며, 이미 전송된 provider 호출은 반환 결과를 회수하고 최소 취소 증거를 남겨 lifecycle cleanup이 후속 처리할 수 있어야 합니다. DB 교체/완료를 먼저 표시한 뒤 늦은 구세대 예약을 방치하면 안 됩니다. 같은 stable ID의 신세대 등록은 구세대 호출 및 정리가 정리된 이후여야 하며, restore의 재등록은 gate가 해제된 새 generation의 현재 DB를 읽도록 합니다. reset은 재시작 정책대로 신규 예약을 막습니다. 기존 cancel use case가 같은 직렬화 경계를 통하지 않아 registry replaceAll과 충돌하는 부분도 범위입니다. 다만 이를 위해 새 전역 작업 프레임워크나 중첩 lock을 만들지 말고 알림 조정 내부의 작은 명시적 coordinator/port로 제한하세요. gate를 소유한 restore가 gate 해제를 기다리는 drain을 다시 await하는 교착을 반드시 테스트합니다. provider 무응답/오류는 정상 완료나 취소 성공으로 바꾸지 않으며, timeout을 추가한다면 native 호출이 실제 취소되지 않았다는 한계를 보존해야 합니다. 완전한 재시작 후 reset 복구 프로토콜은 D03 의존 사항으로 명시하고 이슈 완료 범위를 과장하지 않습니다. 말씀한 잘못된 단일 pass 고정 테스트와 실제 finish/startup/resume unawaited 오류 관찰도 A15 회귀 범위에 포함하세요.

### Q3 — 실패·다음 요청·재시도 종료

**A15 전담 질문**

> A15 Q3 — pass 실패와 후속 요청의 관계를 확정하겠습니다. 예: pass revision 1이 snapshot/OS/registry 단계에서 실패한 동안 revision 2가 접수된 경우 추천은 rev1 대기자를 해당 실패/부분 결과로 끝내고 rev2 이상이 실제 접수돼 있으면 최신 DB로 다음 pass를 실행합니다. 반면 새 요청 없이 오류만 반복되면 dirty를 영구 true로 유지해 무한 재시도하지 않고 멈춰 다음 launch/resume/명시적 retry/실제 mutation에서 다시 시도합니다. 각 pass 시작의 포함 revision cutoff만 완료하고 미래 요청을 오래된 성공·실패로 완료하지 않으며, 실패 후 drain 정리와 새 call 시작 사이의 요청도 유실 없이 처리합니다. OS 성공 후 registry 저장 실패는 armed로 위조하지 않고 가능한 최소 ownership 증거를 보존하며 A14 관측으로 복구합니다(프로세스 종료 사이 crash consistency 전체는 D03). 세대 invalidation은 OS 실패와 구분된 typed outcome/예외로 전달하고 UI는 저장 성공과 예약 실패·무효화를 구분합니다. 이 실패·재시도 계약으로 확정할까요?

**Root 답변**

> Q3 답변: 확정합니다. pass 시작 시 포함한 revision cutoff까지만 해당 결과 또는 실패로 완료하고, 이후 접수된 revision은 다음 pass가 책임집니다. 실패 동안 실제 새 요청이 들어왔다면 최신 DB로 후속 pass를 실행하고, 새 요청 없이 실패만으로 dirty를 재설정해 즉시 무한 반복하지 않습니다. 동일 호출자가 안정된 성공을 기다린다는 이유로 실패 결과를 영구 보류하지 않습니다. drain 종료 정리와 새 요청 접수의 경합은 동기 상태 전이 및 finally에서 명확하게 처리하고 completer별 정확히 한 번 완료를 테스트하세요. generation invalidated는 typed outcome/예외로 구분하고, fire-and-forget 호출부도 오류를 관찰해 unhandled Future를 남기지 않습니다. UI DB 저장 성공을 알림 실패로 롤백하지 않고, 기존 상태 모델에 사실대로 예약 실패/확인 필요를 연결합니다. provider 성공 뒤 registry 저장 실패는 성공으로 위조하지 않습니다. 저장소 자체가 실패해 영속 tombstone 기록조차 불가능하면 메모리상의 최소 ownership과 실패를 유지하고 후속 A14 관측으로 복구하되 프로세스 사망까지 보장한다고 쓰지 마세요. D03의 crash consistency/재시작 복구 의존성을 명시합니다. 회귀는 실제 blocking fake와 Completer를 사용해 snapshot 이후 mutation, 연속 trailing 요청, 실패+새 요청, 실패 단독 종료, finally 경합, generation 교체, stable ID 취소 순서, 교착 부재와 각 호출 Future 결과를 검증합니다.

### Q4 — 실제 요청 진입점·반복 일정·순번의 의미

**A15 전담 질문**

> A15 Q4 — upstream 반복 일정 통합 후 getAlarmWindow가 recurring.materialize(perSeriesLimit:64)를 호출하여 조회 중 DB 쓰기도 발생합니다. 또한 user.dataRevision은 백업 신선도용이며 locale/권한 변경은 포함하지 않습니다. 추천은 A15 request revision을 프로세스 내 요청 순번으로 한정하고 durable dataRevision과 혼용하지 않으며, 재조정 자체의 회차 materialize/registry 갱신이 무한 재조정을 유발하는 전역 DB watcher를 추가하지 않는 것입니다. 실제 성공한 일반 일정 CRUD·준비 완료, 반복 생성/회차·이후 수정/삭제, 알림 설정, permission·locale/timezone 재조회, 시작/resume, restore 완료의 기존 요청 경로를 감사해 접수 유실을 보완하고 60개 최근 eligible coverage를 유지합니다. default/custom preparation 저장도 실제 기존 일정의 준비 시작 시각에 영향을 주는 사용 경로라면 commit 후 동일 진입점에 연결하되 템플릿 등 무관 변경 전반을 watcher로 확장하지 않습니다. recurrence 규칙/backup v2/schema2는 그대로 두고 snapshot→여러 저장→두번째 pass 중 또 저장, nearest 60 경계 교체까지 결정적 회귀를 넣는 범위로 확정할까요?

**Root 답변**

> Q4 답변: 확정합니다. request revision은 메모리상의 접수 순번이며 백업용 durable dataRevision 또는 DB schema version과 별개입니다. 알림 관측/registry 변경, 회차 materialize가 스스로 또 reconcile을 요청하는 전역 watcher는 만들지 않습니다. 열거한 실제 제품 mutation/lifecycle 경로를 감사해 commit 뒤 요청이 필요한 곳만 연결하고, default/custom preparation 편집은 현재 미래 일정의 준비 시작 시각/내용에 실제 영향을 주는 경우에만 포함합니다. 템플릿 라이브러리 같은 무관 변경은 확장하지 않습니다. locale/timezone/permission은 기존 refresh 진입점에서 현재 값을 다시 읽고, 새 periodic polling은 추가하지 않습니다. upstream recurrence 규칙·schema2·backup v2를 유지하며 nearest eligible 60개 대상 교체와 반복 회차 materialize가 drain의 무한 self-dirty를 일으키지 않음을 회귀로 확인하세요. 하나의 pass 안 snapshot의 transaction 일관성 자체가 별도 결함이면 A08/C01과 경계를 표시하되, committed mutation 뒤 실제 새 요청이 접수되면 다음 pass가 최신 상태를 반영한다는 A15의 약속은 보장합니다. 이제 실제 전체 질문·답변을 생략 없이 포함한 상세 문서 작성해 주세요.


## 구현 결과 — 2026-09-24

후속 구현은 root가 A14 소스 커밋 `282db21f5effa86eb2f82c81c247d57a0a6acfd0` 확정 뒤 명시적으로 허용했다. 위 문답과 계획은 원문 그대로 보존한다. 현재 구현 기반 HEAD는 `d2ae69d3f0e139d2d88c46a7f1e0b8da5e403830`이며 A15 소스 커밋과 GitHub 게시·read-back은 root가 별도로 수행한다.

### 적용 내용

- `ReconcileAlarmsUseCase.call()`은 await 전에 메모리 request revision과 대기자를 접수한다. 단일 drain은 pass 시작 cutoff까지만 결과/오류로 완료하고, 도중 접수된 요청은 최신 DB를 읽는 다음 pass로 처리한다. 실패 자체는 재실행 원인이 아니며, cutoff 완료 뒤 idle 전환 사이에는 await가 없다.
- `AlarmOperationCoordinator`가 reconcile, 표적/전체 취소, 개인정보 migration, 복원·초기화 cleanup의 단일 알림 writer다. 기존 gate generation을 사용하고 export의 단순 busy와 실제 replacement를 구분한다. replacement가 구세대 Future를 typed invalidated로 끝내더라도 이미 전송한 native Future가 반환할 때까지 writer 소유권은 유지한다. 호출자의 timeout은 원래 OS Future를 취소하지 않는다.
- OS side effect 전에 최소 ownership을 메모리에 남기고 저장을 시도한다. 취소 실패/저장 실패 기록은 raw 제목·본문·fingerprint 없이 실제 provider/플랫폼 ID와 필요한 시각·timing enum만 유지한다. 성공한 최종 registry write가 메모리 보조 기록을 해제한다. 이는 프로세스 내 보호이며 영속 crash journal이 아니다.
- 취소는 A14의 실제 플랫폼 취소/read-back 계약을 재사용한다. `AlarmRegistrationCleanup`은 취소 미완료를 `AlarmCleanupIncomplete`로 전달하고 tombstone을 보존한다. 실제 저장소 replaceAll/upsert도 Schedule ID 하나로 덮어쓰지 않도록 실제 플랫폼 identity로 dedup한다. fallback은 notification ID, Android native는 requestCode, iOS AlarmKit은 플랫폼의 결정적 UUID 입력인 Schedule ID를 provider와 조합한다. 서로 다른 Schedule 라벨이 동일 OS ID를 가리키면 별도 실예약 두 개로 세지 않는다.
- 한 Schedule에 복수 실예약 기록이 있으면 유지 가능한 하나를 선택하고 나머지 소유권은 취소한다. 실패한 중복은 조용히 버리지 않으며 keeper와 tombstone을 함께 남기고 armed 성공으로 보고하지 않는다.
- 실제 `BackupService.applyRestore`는 replacement gate 안에서 기존 OS 작업 종료와 cleanup을 기다린 뒤 DB transaction을 수행한다. cleanup 실패 시 기존 DB가 유지되고 gate가 다시 사용 가능해져 명시적 restore 재시도가 가능하다. 일반 drain이 gate 해제를 기다리는 교착을 만들지 않는다.
- 실제 `LocalDataResetService.reset`은 reset marker 저장 후 즉시 일반 writer를 무효화한다. 취소 오류를 삼켜 DB close/delete·prefs clear·키 삭제로 진행하지 않는다. marker 이후 실패하면 기존 DB와 취소 증거는 남지만 같은 프로세스의 일반 사용과 reset 재시도는 차단된다. 현 정책상 재시작 복구가 필요하며 D03 전체 복구 프로토콜 완료를 의미하지 않는다.
- 일정 mutation coordinator는 DB commit 뒤 요청을 먼저 접수하고 targeted cancellation 오류를 관찰한다. 실제 default/custom preparation 저장 use case에도 후속 요청을 연결했다. 준비 시작/완료의 직접 cancel 호출은 취소 실패로 이미 commit한 DB 사실과 runtime snapshot/정리를 취소하지 않는다. 구세대 invalidated이면 신세대 runtime을 건드리지 않는다.
- startup/resume와 설정 호출부의 fire-and-forget 오류를 관찰한다. MyPage는 저장된 설정을 유지하면서 예약/취소 불완전 상태를 기존 문구로 표시한다. 전역 watcher, 주기 polling, 새 서버, schema/backup 형식 변경은 없다.

### Root 후속 리뷰와 보완

1. **실제 registry gap**: A14 fake registry만으로는 `AlarmRegistryRepositoryImpl.replaceAll`의 Schedule ID 단일 dedup 유실을 잡지 못했다. 이번에는 실제 SharedPreferences local data source 왕복으로 native와 fallback 두 ID 보존, upsert, 동일 OS ID dedup을 검증했다. reconcile의 기존 단일 map도 keeper 외 소유권을 지우지 않게 보완하고 취소 성공/실패 양쪽을 검증했다.
2. **reset 실패 경계**: 기존 `_cancelAllAlarms().catchError`와 그 뒤 prefs clear가 취소 증거를 지우는 경로를 제거했다. marker 후 gate가 일반 상태로 열리지 않도록 invalidate 시점을 옮기고, 실제 서비스/Drift/secure-store adapter로 원본과 marker·prefs·키 보존 및 일반 writer 차단을 검증했다.
3. **직접 준비 시작/완료 호출자**: cancel이 이제 오류를 전달하므로 DB 시작/완료 뒤 runtime 저장/정리가 중단되는 새 회귀가 없도록 직접 use case 테스트를 추가했다. durable DB 성공을 알림 성공과 혼동하지 않는다.
4. **실제 lifecycle 통합**: 기존 backup/UI 테스트의 `NoopAlarmCleanup`은 격리용 adapter일 뿐 통합 증거로 세지 않았다. 별도 `alarm_replacement_integration_test.dart`는 실제 BackupService/ResetService 및 Drift DB를 쓰고 OS만 blocking fake로 대체했다. 미반환 OS Future 동안 DB 내용이 불변이고 실제 반환·취소 후에만 복원이 교체함을 확인한다.
5. **actual mutation 접수 증거**: 실제 create/update/preparation 테스트에서 저장 뒤 테스트 자체의 추가 reconcile 호출이 접수 누락을 가릴 수 있음을 최종 검토에서 발견했다. 그 호출을 제거하고 use case가 스스로 접수한 pass만 직렬 owner barrier로 기다리도록 강화했다. snapshot 중 삭제 coordinator 테스트도 동일하게 보완했다. 제품 소스는 이 보강 동안 변경하지 않았다.

### 검증 범위와 증거

최종 동결 소스에서 `flutter test --reporter expanded` **795개 통과**, `flutter analyze` **No issues found**를 확인했다. 검증 직전·직후 37파일 hash map은 일치하며 aggregate는 `de887b4e5d34a0ac9c37d62e92a652128fabde3ae1ddcb6dc54d6eccff446dd2`다. generated Dart policy, local-only boundary, `git diff --check`도 통과했다.

원시 로그와 변경 37개 Dart 파일의 전체 SHA-256, 검증 직전·직후 aggregate, 명령별 결과는 `plans/audit-2026-09-23/a15-validation.json` 및 `plans/audit-2026-09-23/artifacts/a15/`에 기록한다. 코드 생성 결과는 로컬에서 재생성했고 ignored/untracked 상태를 검증한다.

| 검증 종류 | 실제 검증한 경계 |
|---|---|
| 최초 RED | 기존 coalescing에서 snapshot 뒤 접수한 호출이 `committed-after-snapshot` 대신 빈 결과를 반환함 |
| barrier/Completer | cutoff별 독립 완료, 3개 연속 pass, 실패+새 요청, 실패 단독 종료, 완료 callback 새 요청, typed invalidation, 늦은 native와 같은 ID 재사용, timeout이 소유권을 풀지 않음, export busy |
| 실제 registry | SharedPreferences 왕복 복수 provider/ID, upsert, 같은 OS ID dedup, 동일 Schedule keeper와 중복 취소/실패 보존, tombstone timing 유지 |
| 실제 restore/reset | Drift 원본/복원 내용, 미반환 OS Future, 취소 실패 뒤 DB/prefs/key/marker/gate 상태, restore 재시도, reset 재시작 필요 상태 |
| 실제 mutation/반복 | create/update와 preparation 저장 use case가 자동으로 접수한 trailing pass, 실제 Drift/반복 materialize와 nearest eligible 60개 교체, self-dirty 반복 없음 |
| 기존/추가 회귀 | cancel 호출자, 준비 시작/완료 snapshot, privacy migration, 상세알림 OFF, MyPage 저장 성공·알림 실패 표시 및 기존 전체 suite |

진행 중 발견된 실패도 남긴다. 최초 전체 실행은 791개 통과/1개 실패였으며 새 저장 후 접수 때문에 늘어난 reconcile 횟수를 정확히 2로 고정한 UI assertion이 원인이었다. 해당 구현 횟수 assertion을 제거하고 실제 설정·취소·상태 표시 assertion을 유지했다. 최소 ownership 전환 후 별도 실행은 62개 통과/1개 실패였으며 cancellation tombstone의 `notificationTiming` 유실을 드러냈다. 기대를 지우지 않고 `ownershipOnly`와 실제 local source sanitizer가 timing enum을 보존하도록 수정하고 실제 왕복 assertion을 추가했다. 최종 mutation fixture는 저장된 civil-time 해석을 명확히 하기 위해 `occurrenceOffsetSeconds: 0`을 지정했다.

### 남은 경계

- 실제 Android/iOS 권한 변경, 잠금 화면, Doze, 재부팅, 도착 시각·중복 개수 검증은 **미실행/device pending**이다. Dart fake와 mocked platform 계약 통과는 OS 전달 증거가 아니다.
- A15는 native 소스를 변경하지 않았다. 이번 검증에서 native SDK 컴파일/APK/Runner build를 다시 실행했다고 주장하지 않는다. A14의 별도 SDK/CI 증거는 root의 해당 receipt를 따른다.
- native Future가 영구 미반환이면 writer/restore cleanup도 대기한다. timeout을 가짜 취소로 취급해 동일 ID를 재사용하지 않는다.
- registry와 메모리 모두 사라지는 프로세스 사망, interrupted reset bootstrap 전체, persistent crash journal은 D03 의존이다. 손상 DB manifest/복구 계약은 D02 의존이며 구현하지 않았다.
- 한 pass 안의 여러 DB 읽기 transaction snapshot 원자성은 A08/C01 경계다. 새로운 commit 뒤 실제 접수된 요청이 다음 pass에 반영된다는 A15 보장을 검증했다.
- 일반 설정 저장 경합의 전면 재설계는 U03 범위다. 이번 변경은 저장 성공을 알림 실패로 롤백하지 않고 관찰 가능한 실패를 남기는 최소 통합이다.

## Root 전달 확인

구현 커밋은 `ecd04293363fa79bd9cd1b140169619dfc083b96`다. Root가37 source/test 파일 및 모든 raw/copied 로그의 SHA256을 독립 대조하고 최종795 tests/analyze 결과를 확인했다. 원격 CI는 push 이후 해당 HEAD 기준으로 별도 확인하며 아직 통과를 주장하지 않는다. 실제 기기 조건이 남아 이슈는 OPEN으로 유지한다.
