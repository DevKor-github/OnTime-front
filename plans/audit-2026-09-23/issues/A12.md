# [P1][A12] 종료 상태의 알림 탭을 준비 완료까지 보존하고 정확한 일정으로 한 번만 진입

## 문제와 사용자 영향

`NotificationService`는 실행 중 `onDidReceiveNotificationResponse`만 등록하고 `getNotificationAppLaunchDetails`를 호출하지 않는다. 따라서 앱이 종료된 상태에서 Android/iOS Fallback Notification을 탭하면 앱은 열리지만 대상 일정 진입 의도가 사라질 수 있다. 실행 중 callback도 `NavigationNotificationTapRouter`에서 곧바로 `NavigationService.push`로 전달되어 DB/Local Profile/router 준비 전에 실패하거나 startup/onboarding/permission gate redirect에서 유실될 수 있다.

별도의 자체 native alarm launch 처리에는 pending map이 있지만 Auth authenticated와 navigator context만 확인하고 push 전에 pending을 지운다. notification/alarm gate 해소, 초기 launch와 callback의 수신 순서, 데이터 교체, 중복 전달은 공통으로 다루지 않는다. 플러그인의 launch details를 단순히 한 번 추가하는 것으로 끝내지 않고 진입 의도의 수명과 준비 상태를 함께 고친다.

감사 우선순위는 **P1**이다. 누락과 위험 경로는 코드로 확인했고 Android/iOS 종료→탭 현상의 실제 기기 재현은 아직 수행하지 않았다. 이 문서는 설계 및 실제 문답 기록이며 구현 완료나 기기 통과 증거가 아니다.

## 조사 기준과 근거

감사 기준 SHA `44067d7ab26b6290c16cdb13b99f57387dec08f6`; 구현 착수 기준은 A11 커밋 `089afee51b2572339b4650bd3679b1a1ef72781e`이다. A11 진행 중에는 읽기 전용으로 조사했고 기존 변경을 수정하지 않았다.

| 근거 | 확인 내용 |
|---|---|
| `lib/core/services/notification_service.dart:216–252` | plugin initialize와 callback만 있고 launch details 조회가 없다. bootstrap privacy cleanup도 동일 setup을 호출할 수 있어 delegate/router보다 초기화가 앞설 수 있다. |
| `lib/core/services/notification_tap_router.dart` / `navigation_service.dart` | callback을 즉시 push하고 navigator context를 강제 역참조한다. readiness, pending, 중복 진입 관리가 없다. |
| `lib/presentation/app/screens/app.dart:116–152` | 자체 native pending은 Auth authenticated+context만 검사하며 push 전에 삭제한다. |
| `lib/presentation/shared/router/go_router.dart:255–290` | Auth authenticated여도 notification/alarm gate가 unresolved/required이면 startup/권한 안내로 redirect된다. |
| `lib/presentation/shared/router/go_router.dart:305` 이후 | 일정 시작 route는 특정 ID 검증을 요청하나 null/mismatch에 LoadingScreen을 표시한다. terminal rejection과 transient 조회 실패를 구별해 무한 loading을 막아야 한다. |
| `lib/core/services/alarm_scheduler_service.dart:166–221` | 자체 native callback과 `getLaunchPayload` polling은 plugin launch details와 별도이다. 두 입력이 같은 사용자 진입을 중복 push하지 않게 연결할 필요가 있다. |
| `lib/core/database/local_data_operation_gate.dart` | 이미 generation이 존재한다. 데이터 교체 작업 시작과 invalidate에서 증가한다. 새 DB 세대 체계를 추가하지 않는다. |
| `lib/core/backup/backup_service.dart:171`, `local_data_reset_service.dart:25` | restore/reset이 기존 gate의 `replacesData: true` 경계를 사용한다. |
| `lib/main.dart`, `bootstrap_privacy_boundary.dart` | runApp 이전 bootstrap 실패가 App 자체 표시를 막는 D01 문제는 별도로 남는다. A12가 이 복구 UI 문제까지 해결했다고 주장하지 않는다. |

고정 `flutter_local_notifications 20.1.0`의 로컬 설치 소스를 직접 확인했다. `lib/src/flutter_local_notifications_plugin.dart:103–108`은 실행 중 callback과 앱 launch details를 구별한다. Android `FlutterLocalNotificationsPlugin.java:1682–1700`은 현재 Intent의 launch response를 반환하고 iOS `FlutterLocalNotificationsPlugin.m:203–211`은 보관 중인 launch response map을 반환하며 읽었다고 소비하지 않는다. iOS L1061–1085는 초기화 전 응답과 초기화 후 callback 경로를 구별한다. 이 응답에는 전역 유일 tap event ID가 없으므로 전 프로세스·전 기기에 걸친 exactly-once를 약속하지 않는다.

## 도메인 및 기존 결정

- Local-only OnTime은 Android/iOS 제품이며 네트워크·서버·로그인·원격 push를 추가하지 않는다(ADR 0011, 0016).
- Fallback Notification을 통해서도 현재 Schedule에 진입할 수 있어야 한다(ADR 0010).
- Recovery Mode에서는 일반 제품 화면으로 이동하지 않는다. 같은 DB 재시도와 Backup Restore/Local Data Reset을 구별한다(ADR 0023).
- A11의 최소 allowlist, 현재 DB 재검증, 명시적 시작 확인, `requiresConfirmation` 보호를 유지한다. 알림 payload는 준비 시작/완료 명령이 아니다.
- pending tap/receipt sequence/generation은 일반 구현 개념이다. CONTEXT에 새 도메인 용어를 넣지 않는다. 기존 정책을 지키는 되돌릴 수 있는 구현 수정이므로 새 ADR도 만들지 않는다.

## 확정 범위와 설계

### 1. 초기 launch와 실행 중 callback의 수집

1. Android/iOS plugin 초기화 후 공개 `getNotificationAppLaunchDetails`를 조회한다. `didNotificationLaunchApp` 및 유효 response가 있을 때만 최소 route 정보를 제출한다. null/false/손상 payload는 일반 시작을 방해하지 않는다.
2. 성공 응답(null 포함)을 받은 뒤에는 같은 서비스 수명에서 초기 launch 조회를 반복하지 않는다. 실패에는 완료 표시를 하지 않고 callback 수신을 유지한다. 다음 명시적 initialize/resume/retry가 재시도를 할 수 있다.
3. 동시 setup/initialize/launch 조회는 single flight로 묶는다. 반복 초기화가 callback을 중복 설치하거나 같은 초기 진입을 반복하지 않아야 한다.
4. 초기 조회를 시작할 때 receipt sequence와 `LocalDataOperationGate.shared.generation`을 캡처한다. 반환 전에 callback/native로 더 최신 의도 B를 받았거나 데이터 세대가 바뀌면 늦은 초기 A를 버린다. 느린 cold 조회가 최신 사용자 의도를 덮지 않는다.
5. 입력 경계부터 A11 route allowlist로 축소한다. raw payload, 준비 이름, 과거 fingerprint, arbitrary start 명령을 보존하거나 로그로 내보내지 않는다.

### 2. pending과 준비 상태

1. DB/Local Profile, onboarding, notification/alarm gate, 실제 router가 준비되기 전에는 마지막 형식상 유효한 Schedule 진입 의도 하나만 현재 프로세스 메모리에 보관한다.
2. A→B 순서로 탭하면 B를 보관한다. malformed/unknown 입력은 기존 유효 pending을 덮지 않는다. 이후 B가 없어져도 A나 nearest 일정으로 돌아가지 않는다.
3. Auth authenticated만으로 준비 완료로 간주하지 않는다. 실제 router redirect가 해소된 상태에서만 전달하며 gate에 진입한 것을 성공 소비로 처리하지 않는다.
4. 알림 권한을 거부했더라도 안내 gate를 통과한 이후에는 이미 받은 탭을 현재 DB로 검증하고 확인 화면을 열 수 있다. 탭 처리 때문에 새 권한 요청을 추가하지 않는다.
5. 별도 SharedPreferences/DB 영속 큐는 추가하지 않는다. 프로세스가 다시 종료되면 OS가 다음 launch에 제공하는 정보로 새로 수집한다.

### 3. 현재 DB의 정확한 대상과 실패 구별

1. 준비 완료 후 해당 ID가 현재 DB에 존재하고 unfinished인지 확인한다. payload의 과거 데이터나 현재 nearest Schedule로 대체하지 않는다. domain use case/repository 경계를 유지한다.
2. 존재·미완료가 확인된 일정의 최신 시작 확인 화면만 연다. 탭 자체로 준비 시작, 완료, outcome 생성, 자동 재시작을 수행하지 않는다.
3. definitive not found/completed이면 의도를 소비·거절하고 안전한 현재 화면을 유지하거나 home으로 종료한다. LoadingScreen에 무한히 남거나 다른 일정을 표시하지 않는다.
4. DB 일시오류는 not found와 구별한다. 같은 generation의 최소 pending을 유지하고 readiness 회복/명시적 retry에서 다시 검증한다. 필요한 경우 catch-all not-found를 최소한의 typed outcome 경계로 분리한다.
5. raw payload/내부 예외를 사용자 문구로 표시하지 않고 자동 무한 polling을 추가하지 않는다.

### 4. 중복 수신 및 데이터 세대 교체

1. 동일 schedule+route 종류가 pending/inflight/현재 같은 확인 화면에 있으면 중복 push하지 않는다. 초기 launch, callback, 자체 native 경로가 같은 의도를 전달하는 경우도 포함한다.
2. 그 확인 화면을 나간 뒤 새로운 사용자 callback 탭이 오면 다시 열 수 있다. 영구 schedule-ID blacklist나 임의 시간 TTL로 별도 의도를 차단하지 않는다.
3. ‘한 번만’은 현재 프로세스에서 같은 진입 의도를 중복 push하지 않는 범위다. OS가 tap ID/consume를 보장하지 않으므로 글로벌 exactly-once라고 부르지 않는다.
4. 같은 DB의 Recovery retry는 메모리 pending을 유지하며 Recovery/onboarding 동안에는 이동하지 않는다.
5. restore/reset으로 기존 operation gate generation이 바뀌면 pending, inflight 검증, 늦은 cold 조회 결과를 모두 무효화한다. 복원 데이터가 우연히 같은 schedule ID여도 이전 탭으로 자동 진입하지 않는다.
6. 모든 비동기 검증/전달은 수신 sequence와 데이터 generation을 완료 시점에도 재확인한다. 새로운 B나 새 데이터 세대 위에 오래된 A의 결과가 적용되지 않아야 한다.

## 수용 기준과 자동 회귀

| 시나리오 | 기대 결과 |
|---|---|
| plugin launch true + 유효 Schedule payload, 느린 DB/router | 준비 전 push 0회, 준비 후 정확한 ID 확인 화면 1회 |
| launch false/null/손상 JSON/unknown/빈 ID | 예외/임의 일정 진입 없이 일반 시작; 기존 유효 pending 유지 |
| setup/initialize 동시 호출과 반복 호출 | plugin 초기화와 초기 조회 single flight, 성공 후 초기 launch 중복 수집 없음 |
| 초기 조회 실패→callback→명시적 retry | callback 수신 유지, 실패를 완료로 표시하지 않음, 중복/오래된 결과 방지 |
| 느린 launch A 조회 중 callback/native B | B가 최신 의도이며 A 반환이 B를 덮지 않음 |
| 준비 전 A→B→malformed | B 하나만 유지, 준비 후 B만 진입 |
| B가 대기 중 삭제/완료됨 | B 거절/소비, A/nearest fallback 없음, 무한 loading 없음 |
| 동일 의도가 launch/callback/native로 중복 수신 | pending/inflight/현재 확인 화면에서 push 1회 |
| 확인 화면에서 나간 뒤 같은 일정의 별도 탭 | 새 진입 허용, 영구 중복 제거 없음 |
| Auth 준비됨 + notification/alarm gate 미해소 | gate에서 의도를 소비하지 않고 해소 후 대상 확인 |
| 알림 권한 거부 후 gate 해소 | 추가 권한 요청 없이 기존 탭의 대상 확인 가능 |
| Recovery→같은 DB retry | Recovery navigation 0회, 성공 후 현재 DB 재검증 |
| 대상 조회 일시 실패→retry | not found로 오판하지 않고 같은 generation pending 보존 |
| restore/reset이 pending/DB 조회/cold 조회 중 발생 | 이전 세대 결과로 navigation 0회, 같은 ID 재등장에도 무효 |
| legacy payload에 원문/fingerprint/start 명령 포함 | 최소 route만 사용, 로그/재저장 원문 없음, 자동 시작 없음 |

플랫폼 fake 테스트는 plugin 초기 응답/오류/동시 조회/callback 경합을 검증한다. coordinator 단위 테스트는 last-valid-wins, dedupe, generation, 오류 분류를 검증한다. 실제 router+gate+대상 확인 화면을 포함하는 widget 회귀는 redirect 후 의도 유실 및 잘못된 Schedule 표시를 검증한다. 기존 알림 routing, A11 privacy, 일정 시작 확인 회귀도 실행한다. 단순 fake navigation 호출만으로 전체 UI 경로가 통과했다고 판단하지 않는다.

기본 검증은 필요한 생성 산출물 재생성(비추적), 변경 Dart format, 관련 Flutter tests, flutter analyze이며 통합 변경에는 전체 Flutter suite를 실행한다. 실제 기기 증거는 자동 테스트와 별도 기록한다.

## 실제 기기 검증 계획과 현재 한계

Android와 iOS 각각 Fallback Notification을 실제 예약하고 프로세스 종료→알림 탭→정확한 일정 시작 확인 화면을 기록한다. ordinary app icon cold start, 실행/백그라운드 상태 탭, 느린 startup/gate, 삭제·완료된 일정, 화면 이탈 후 재탭, native와 fallback 중복 수신을 가능한 OS 조건에서 확인한다. OS 버전·앱 빌드 SHA·기기·예약/탭 시각·기대/실제 화면·중복 화면 수를 증거로 남긴다. 앱 프로세스 종료와 Android 설정의 force-stop은 구별하고 force-stop 상태의 OS 알림 전달을 일반 종료 보장으로 과장하지 않는다.

현재 기기 시나리오는 **미실행**이다. fake/widget 통과가 향후 확인되어도 실제 Android/iOS cold tap 검증은 별도 pending으로 표시한다. D01의 runApp 이전 bootstrap 실패/복구 UI 문제, OS가 보관하지 않는 종료 후 tap의 영속 복원, 글로벌 exactly-once 보장은 A12 완료 주장에 포함하지 않는다.

## 의존성 및 제외 범위

- A11의 privacy allowlist/confirmation/current-DB 정책 위에 구현한다. 기존 변경을 되돌리지 않는다.
- A01–A05의 플랫폼 등록/재등록/노출 정책은 보존하고 알림 scheduling redesign이나 새 permission 흐름은 만들지 않는다.
- D01 startup recovery UI, reset/restore 전체 원자성 문제는 별도이며 A12는 기존 generation과 lifecycle 경계에 연결한다.
- 서버·로그인·네트워크·원격 push·분석 이벤트·백그라운드 메시징·플러그인 fork·DB schema 또는 영속 tap queue 추가는 제외한다.
- 구현/원격 이슈 생성은 root의 별도 지시 후 수행한다. 이 문서는 GitHub 이슈 본문으로 사용하도록 준비했다.

## grill-with-docs 전체 실제 문답

전담 에이전트 `/root/grill_a12`가 `AGENTS.md`, `grill-with-docs/SKILL.md`와 CONTEXT/ADR 형식 지침, `CONTEXT.md`, 관련 ADR, 감사 A12, 상태 파일, A11 합의, 앱 및 고정 plugin 소스를 읽었다. 코드로 답할 내용은 직접 조사했다. 아래 결정 질문은 **한 번에 하나씩 보내고 root 답변을 받은 뒤 다음 질문**을 보냈다. 사용자 지시에 따라 root가 대리 응답했다. 아래는 실제 전달된 질문과 응답 전문이다.

### 질문 1 — 서브에이전트

> A12 Q1. DB/Local Profile와 라우터가 아직 준비되지 않은 동안 사용자가 일정 A 알림을 탭한 뒤 일정 B 알림도 탭하면, 어느 진입 의도를 보존할까요? 권장: 마지막 유효한 사용자 탭 B 하나만 보존하고 준비 완료 후 B의 최신 시작 확인 화면을 한 번만 엽니다. 앞선 A까지 연속 push하지 않고 malformed 입력은 기존 유효 pending을 덮지 않습니다. 알림 탭 자체로 준비 시작·완료를 수행하지 않는 A11 규칙은 그대로 적용합니다. 이 정책으로 확정할까요?

### 답변 1 — root

> A12 Q1 답변: 권장정책 확정합니다. DB/Local Profile와 router ready 전에는 마지막 형식상유효 schedule tap 한 개만 보존하고 A→B면B를채택합니다. malformed/unknown payload는 기존유효pending을덮지않습니다. 준비완료후 currentDB에서해당ID 존재·unfinished를확인해그일정의시작확인화면을한번만엽니다. B가그사이삭제/완료되었으면B를거절하고버리며 예전A/nearest일정으로대체하지않습니다. tap은준비시작/완료명령이아니며 A11confirmation marker 및privacy allowlist를유지합니다. foreground/background/cold/native/fallback 중같은의도가중복수신돼도현재진입1회, 이후실제별도사용자탭은영구dedupe하지않아야합니다.

### 질문 2 — 서브에이전트

> A12 Q2. 시작 중 DB 오류로 Recovery Mode에 들어간 뒤 ‘같은 DB 다시 시도’하거나 ‘백업 복원/로컬 데이터 초기화’로 빠져나올 때 pending 탭을 어떻게 처리할까요? 권장: 같은 DB 재시도에는 프로세스 메모리의 최소 payload를 유지하되 Recovery 중에는 이동하지 않습니다. Backup Restore/Local Data Reset으로 데이터 세대가 교체되면 pending을 폐기해 복원된 우연히 같은 ID로 자동 이동하지 않습니다. 앱 프로세스 재종료까지 별도 영속 큐로 보장하지 않고 다음 프로세스의 OS launch details만 다시 받습니다. 이 수명 경계가 맞을까요?

### 답변 2 — root

> A12 Q2 답변: 권장수명경계 확정합니다. 같은DB retry는 현재프로세스의메모리 최소payload 1개를유지하고 Recovery/onboarding/DB/router미준비에는 navigation하지않습니다. restore/reset으로LocalDataLifecycle generation교체시 pending과inflight delivery를모두무효화하고 우연히같은ID를 가진새데이터로이동시키지않습니다. 별도SharedPrefs/DB 영속큐를추가하지않으며 프로세스종료까지exactly-once보장이라고주장하지않습니다. 다음프로세스는OS launchdetails로다시받습니다. D01의pre-runAppbootstrap실패로App자체가안뜬현상은별도의존성으로명시하고 A12가전부해결했다고하지마세요. readiness상태/데이터generation 변화와동시에들어온tap도 generation을캡처해현재세대검증후라우팅해야합니다.

### 질문 3 — 서브에이전트

> A12 Q3. 고정 plugin 20.1.0은 launch details를 읽어도 OS 쪽 값을 소비하지 않으며 고유 tap event ID도 주지 않습니다(Android는 현재 Intent, iOS는 launch response map 반환). ‘한 번만’ 범위를 권장대로 정밀화할까요? 초기 launch details는 성공 조회 1회만 수집하고 callback/native와 같은 schedule 진입이 pending/inflight/현재 열림이면 중복 push하지 않습니다. 이미 화면을 나간 뒤 발생한 새 callback 탭은 다시 허용합니다. 늦게 반환된 초기 launch 조회가 먼저 수신한 더 최신 callback B를 A로 덮지 않도록 수신 순서/generation을 검사하며, 영구 schedule-ID blacklist·시간 TTL 추측으로 새 의도를 막지 않습니다. readiness는 Auth authenticated만이 아니라 notification/alarm gate redirect까지 해소된 router 상태로 판단합니다.

### 답변 3 — root

> A12 Q3 답변: 제안한정밀범위 확정합니다. OS는tap event ID/consume 보장없으므로 globallyexactly-once라고표현금지. 초기launchdetails는성공조회1회(실패는재시도가능)이며중복init/concurrentcalls단일flight로묶습니다. 초기조회시작시capture한receipt sequence+datageneration을반환후검사해그뒤callback/nativeB가왔으면늦은A폐기합니다. 동일schedule+route종류가 pending/inflight/현재같은확인화면이면중복push금지, 해당화면을나간뒤새callback은다시허용. 영구IDblacklist/임의시간TTL없음. DB/profile/authenticated뿐아니라온보딩·notification/alarm gate redirect해소와router실제준비를관찰하고, gate에서소비완료처리하지않습니다. notification권한을거부해도설정gate통과후기존tap의currentDB검증/확인화면진입은허용합니다. 닫힌/삭제된일정은pending을소비거절하고다른일정으로fallback하지않습니다. A11commit089afee5 push중이며A12구현은이커밋위에서수행할준비가능합니다. 더미실기기통과없이platformfake+widget/coordination회귀와실기기pending을구분해주세요.

### 질문 4 — 서브에이전트

> A12 Q4. launch details 조회 실패나 준비 완료 후 대상 DB 조회가 일시적으로 실패할 때 사용자 의도를 언제 폐기할까요? 권장: plugin launch 조회는 성공했을 때만 초기 수집 완료로 표시하고 단일 flight로 제한해 다음 명시적 초기화/시작 재시도에서 재조회합니다. callback 수신은 계속 작동해야 합니다. DB 오류는 삭제/완료로 간주하지 않고 최소 pending을 같은 generation에서 유지하며 Recovery/명시적 재시도 뒤 다시 검증합니다. DB가 실제 없거나 완료라고 확인된 경우에만 의도를 소비·거절하고 안전한 현재 화면/home으로 종료합니다. 자동 무한 polling, raw payload/내부 예외 사용자 노출, 새 권한 요청은 추가하지 않습니다. 이 실패 정책으로 확정할까요?

### 추가 코드 조사 — 서브에이전트

> A12 조사 보충: 이미 LocalDataOperationGate.shared.generation이 있고 BackupService.restore(...replacesData: true), LocalDataResetService.reset(...replacesData: true)/invalidate에서 증가합니다. 새 DB 세대 개념을 만들지 않고 이 기존 gate를 연동하면 됩니다. 현재 App native drain은 Auth authenticated+context만 검사하고 push 전에 pending을 지워 notification/alarm gate redirect에서 유실될 수 있습니다. 따라서 fallback launch 추가만으로 끝내지 않고 공통 readiness/중복 진입 경계를 검증해야 합니다. 새 도메인 용어/ADR은 필요하지 않은 구현 수정입니다.

### 답변 4 — root

> A12 Q4 답변: 제안실패정책확정합니다. plugin launch초기수집은성공응답(null포함) 때만완료, 실패시callback수신유지+다음명시적initialize/resume/retry시singleflight재시도합니다(무한polling금지). 대상DB 일시오류와 definitive notfound/completed를구분하고 전자는같은generation pending유지하며 readiness회복/명시적retry에검증, 후자는소비거절하되현재안전한화면유지나home으로종료. 다른일정fallback/auto-start없음. 내부예외/rawpayload 사용자노출·추가권한요청없음. source code의catchallnotfound를별도typedoutcome으로분리할필요있으면최소경계로수정하고 meaningfulfailure회귀추가. 기존LocalDataOperationGate.shared.generation재사용확정, restore/reset세대변화로pending/inflight/coldreturn모두무효화합니다. 질문충분하면실제4Q&A전부포함A12.md상세scope/우선순위/AC/테스트/미검증기기/의존성 작성후root에게보내주세요. Root issue생성readback후구현지시하겠습니다.
