# iOS UI quality / Figma parity handoff

2026-09-23 checkpoint. Branch: `codex/ios-ui-quality-figma-parity`, originally based on committed `feature/figma-offline-screen-sync` commit `44067d7ab26b6290c16cdb13b99f57387dec08f6`. This isolated worktree did not copy changes from other worktrees. The remote feature branch's offline recurring-schedules commit `f0877d1d` was merged cleanly after the initial UI QA commits. Generated Drift/Freezed/etc. local outputs were regenerated; post-merge analyzer and full tests pass.

Source Figma file: `FIdR6bUMHScn9FWbwqrBsA` (`OnTime-Design-System`), page `1:25`, board `285:5`. The old `GiDCK...` link is not the review source. Reference viewport: Korean iOS 390×844.

## Current result

- `docs/design/figma_code_map.yaml` inventories 21 active router paths and 44 Figma variants, including the newly merged recurring-schedules board. Five route defaults and 16 variants have reviewed captures and Flutter goldens. Home, Calendar, My Page, and Home loading/error are in progress; five app-only routes are excluded from Figma visual parity; eight routes and 26 variants remain pending. My Page moved from reviewed to in progress because upstream added a recurring-management row. `dart run tool/report_figma_ui_coverage.dart --strict` is intentionally not passing yet. The excluded routes still need normal app behavior tests.
- My Data default: Figma node `1948:12072`, 358×68 card/row geometry and route actions checked. Recovery default `1957:11762` and reset complete `1963:11762` were adjusted to source icon/text/button positions. My Data reset confirmation `1977:11817` and recovery reset confirmation `2003:12059` use the shared 277px dialog. Native status bar and home indicator remain outside Flutter widget goldens; minor font rasterization differences remain.
- The shared dialog and action buttons now use the source card width and colors. Buttons are 44px high for the iOS tap-target check; Figma's visible button reference is 43px. A recovery reset failure now clears the busy state and shows an error message.
- My Page default now matches the Figma 91px top chrome, 116px alarm section, 108px data section, and bottom navigation action placement. Its selection controls keep 44px tap surfaces around the visible 24px rings. Local notification opt-in behavior remains covered by the original widget tests.
- My Data password create/input/invalid-length/mismatch and restore-preview overlays now use the reviewed 277px card at the 352px Figma top position. Secure fields remain empty for a new password; the goldens enter Figma's 15- or 9-character fixture values before capture. A canceled dialog never starts export/restore, invalid values do not export, and 2× text/keyboard checks passed. Backup timestamps use the Korean format shown in the preview source.
- All My Data freshness states and its busy and result-message states now have reviewed captures. The Figma action rows have an 18px gap without the earlier divider. Disabled rows render at 38% opacity; the busy spinner uses the exported 40px vector artwork at y466. The shared 358×44 snackbar appears at y752. The export path is tested for pending, success, and failure; restore-success snackbar parity is visual only until the native restore flow runs.
- Recovery busy reuses that spinner at the Figma y585.5 position, dims and disables both actions, and returns to an actionable error state when reset fails. The Figma busy PNG omits the storage glyph specified in its node context; Flutter preserves the existing default-screen icon and records this exception in the manifest.
- Privacy Policy now uses the source's 60px title bar and 350px body grid starting at x20/y122. Its full local-only policy content remains scrollable at 2× text. Pretendard is used where the Figma screenshot specifies Noto Sans KR, which is not bundled in the app.
- Preparation/spare-time editing now matches the source's header, 46px stepper controls, 358×62 rows, minute labels, and 32px visible add button with a 44px touch surface. The six linked rows are a screenshot fixture, not a row limit; adding a seventh is tested. Save/edit/failure tests and shared list-field tests pass.
- Home's regular 390×844 hero, exported mascot, scheduled today tile, month header/weekday placement, and route-tinted bottom bar were compared with source captures. Full-shell goldens now cover default, loading, and error. Loading masks only the month region and blocks date taps; error retry requests the displayed month. Figma titles its grid December 2024 but draws November 2024 dates, including the selected 21 in the fourth rather than third week. Flutter keeps correct December date arithmetic; exact parity for these three states awaits a product decision. The compact layout still has its own behavior tests.
- Calendar's Korean 390×852 empty state now has the source's soft background, centered title, 354×390 card at x18/y111, selected-date heading, empty message, and 149×44 add action. A test-only reference date lets the sample 2024-12-21 show its add action while production still hides it for past dates. A golden and card/touch geometry assertion cover the screen; all 12 Calendar screen tests pass. Its December label also sits above the incorrect November date grid in Figma, so parity remains in progress.
- The merged recurring feature adds a My Page management row, a `/recurringSchedules` route, and 21 Figma frames. The 390×844 My Page golden was refreshed to track current code, but its new row has not yet been compared to Figma `2113:21761`; the old default capture `965:4676` no longer covers the full screen. The recurring route and creation flow nodes are inventoried as pending, not passed.
- After the recurring feature merge, build_runner regeneration, Flutter analysis, local-only product boundary, generated-Dart policy, manifest validation, and all 636 Flutter tests passed with `--coverage --concurrency=1`. Coverage is 85.60% (10561/12338 lines), above CI's 80% minimum. The updated My Page golden passes but is not yet visually approved against new Figma `2113:21761`; other reviewed goldens were compared with Figma references and document their exceptions.

## Native test constraint

An iPhone 16 iOS 18 simulator was booted, but `flutter run --no-resident` failed twice while Xcode reported `No space left on device`. The host had 116 MiB free initially; generated Xcode/VSCode/Playwright caches were removed, reaching about 1.6 GiB free, but the build again filled the volume. Later widget tests also intermittently ran out of space; generated Xcode compilation cache and Chrome's re-downloadable Cache/Code Cache were removed to let the 587-test run finish. During the latest Home work, free space again dropped below 400 MiB. Deleting only this worktree's regenerable `build/test_cache` and `build/unit_test_assets` temporarily allowed focused goldens to pass. Another worktree's Xcode DerivedData and all simulator data were left untouched. Do not report an iOS app launch, permissions flow, screenshot, or performance result from this checkpoint. A later attempt needs several GiB of free space without erasing user simulator data or device support files.

## Next work

1. Resolve the Figma Home/Calendar month-title/date-grid contradiction with product guidance. Continue deterministic Flutter fixtures for Calendar's seven remaining variants and remaining routes without falsifying calendar dates. The Calendar empty fixture's reference clock is test-only. Source PNGs for these paths are saved and hash-checked in the manifest.
2. Add native behavior and accessibility checks for export/restore and reset failures. Resolve Home loading/error sign-off after the date-grid decision, then review Calendar expanded/list/swipe/delete/loading/error states.
   Figma desktop context for Calendar loading/error nodes repeatedly timed out on 2026-09-23; browser navigation to one of those nodes also timed out. Do not infer their visual details from the empty-state capture.
3. Add iOS integration scenarios for schedule create/edit/delete, backup/restore/reset, recovery and permissions once the simulator build is possible. Profile scrolling/navigation on an attached iPhone. Do not infer native correctness from widget goldens.
4. Re-run `flutter analyze --no-pub`, `flutter test --no-pub --concurrency=1`, `dart run tool/check_local_only_boundary.dart`, and `dart run tool/report_figma_ui_coverage.dart`. Complete strict coverage only after each design route and variant is genuinely reviewed or explicitly excluded with a reason.
